package usf.saav.howard;


public class geodesicDistance 
{
	/*
	public static void main(String args[])
	{
		ArrayList<Point> Points = new ArrayList();
		
		if (mousePressed() )
		{
			Point temp = new Point(mouseX, mouseY, Points.size() + 1);
			Points.add(temp);
		}
	}
	
	*/
	
}
/*
static void mousePressed()
{
	int mouseX, mouseY;
	// I would try and insert code here if it would let me
}

float averageGeodesic(Point passed)
{
	// passed.neighbors and their immediate neighbors
	return 1.1f;
}

// have to make sure it doesn't try to add edges between the same point 
boolean withinRange()
{
	if (mousePressed() )
	{
		if ( math.abs(mouseX - point.x) < 5 && math.abs(mouseY - point.y) < 5 )
		{
			return true;
		}
	}
}
*/